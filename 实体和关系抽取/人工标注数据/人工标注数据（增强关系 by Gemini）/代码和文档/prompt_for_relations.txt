# 角色
你是一位专注于从飞机phm领域文本中识别实体间结构化关系的专家。

# 任务
你的唯一任务是：基于下面提供的【论文全文】，找出【预先标注的实体列表】中实体之间存在的关系。

# 关系定义 (Schema)
你必须严格按照以下格式和类型定义来识别关系：
{schema_placeholder}

# 规则与约束
1.  **绝对禁止**：不许创造任何新的实体，所有关系中的“头实体(head)”和“尾实体(tail)”的文本，必须**一字不差**地从【预先标注的实体列表】中选取。
2.  **专注关系**：你的输出中**只需要包含`relations`列表**，不需要再重复输出`entities`。
3.  **无关系则为空**：如果在实体之间找不到任何符合定义的关系，请输出一个空的`relations`列表：`[]`。

# 输入数据

## 【论文全文】
{full_text_placeholder}
【预先标注的实体列表】
{entities_placeholder}

## 输出格式要求
请严格按照以下JSON格式输出，只包含一个名为 "relations" 的键：
{
  "relations": [
    {
      "type": "关系类型",
      "head": "头实体文本",
      "tail": "尾实体文本"
    }
  ]
}